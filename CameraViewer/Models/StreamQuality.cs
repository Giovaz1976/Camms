namespace CameraViewer.Models
{
    /// <summary>
    /// Calidad del stream de video
    /// </summary>
    public enum StreamQuality
    {
        Main = 0,    // ch00_0 - Alta calidad (HD)
        Sub = 1      // ch00_1 - Baja calidad (SD)
    }
}
